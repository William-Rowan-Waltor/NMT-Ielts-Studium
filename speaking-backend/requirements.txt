fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
faster-whisper>=1.0.0
httpx>=0.27
python-dotenv>=1.0

# Optional: on-device TTS for REAL Listening audio (powers POST /api/tts).
# Install with:  pip install "supertonic[serve]"   (downloads a ~99M ONNX model on first use)
# supertonic>=1.3.0
